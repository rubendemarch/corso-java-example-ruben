<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- QUESTO E' IL FILE CHE COMPONE LE QUERY -->
<mapper namespace="brand">
	<select id="list" resultType="HashMap">
		select * from brands
	</select>
	<insert id="add" parameterType="HashMap">
		INSERT INTO brands
		(ID_BRAND,
		IS_VISIBLE,
		URL,
		LOGO_URL,
		NAME,
		IS_DELETED)
		VALUES(#{ID_BRAND},#{IS_VISIBLE},
			#{URL,jdbcType=VARCHAR},
<!-- 			IL TIPO null DA' PROBLEMI A IBATIS, QUINDI SI SPECIFICA IL JDBCTYPE -->
			#{LOGO_URL,jdbcType=VARCHAR},
			#{NAME},#{IS_DELETED})
<!-- 	se non gli passo qualche valore mette null , che non è un problema se nel db la colonna è nullable-->
	</insert>
	<update id="update" parameterType="HashMap">
		update languages SET IS_VISIBLE=#{IS_VISIBLE},
		IS_DELETED=#{IS_DELETED},
		URL=#{URL},
		LOGO_URL=#{LOGO_URL},
		NAME=#{NAME}
		WHERE ID_BRAND=#{ID_BRAND}
	</update>
</mapper>